services: 
  db: 
    image: postgres:15
    environment:
      # - host=db
      # - port=5432
      # - name=servidor-local
      # - user=root
      # - password=Webpass2334!
      - POSTGRES_PASSWORD=Webpass2334!
      - POSTGRES_USER=root
      - POSTGRES_DB=servidor_local
    ports: 
      - "5432:5432"
    #volumes: 
    # - postgres_data:/var/lib/postgresql/data
  
  api: 
    build: .
    # image: servidor-local
    environment:
      - DATABASE_URL=postgresql://root:Webpass2334!@db:5432/servidor_local
      - DB_PG_HOST=db
      - DB_PG_PORT=5432
      - DB_PG_NAME=servidor_local
      - DB_PG_USER=root
      - DB_PG_PASSWORD=Webpass2334!
      - PORT=8080
      - JWT_SECRET=lemnrgbeurbht3h48t58y4375yt9t04h18hbh7u1h28439h842p8hge8932u4t09werhg29p834uy92847hwb7gt8985y72349259324hgewgbebgjwqithhgui9ewrht834
    depends_on:
      - db
    ports: 
      - "8080:8080"
    volumes: 
      - ./src:/app/src 

  k6_attack: 
    image: grafana/k6:latest
    volumes:
      - ./src:/app/src
    command: ["sleep", "infinity"]
    depends_on: 
      - api 
    ports: 
      - "5665:5665"
    environment: 
      - K6_WEB_DASHBOARD=true
      # - K6_WEB_DASHBOARD_HOST=0.0.0.0
    
  



  
    
