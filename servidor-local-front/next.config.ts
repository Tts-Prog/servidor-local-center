import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "plus.unsplash.com", "cons.unsplash.com", "ththt.unsplash.com"],
  },
};

export default nextConfig;
